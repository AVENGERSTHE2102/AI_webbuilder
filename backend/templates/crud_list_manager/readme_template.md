# {ENTITY_NAME} Manager

**Auto-generated by Zero-Code AI App Builder**

{DESCRIPTION}

---

## 🚀 Run Instantly (3 Commands)

### Prerequisites
- **Node.js 18+** ([Download here](https://nodejs.org))
- **Python 3.8+** (Usually pre-installed on Mac/Linux)

### Backend Setup (Terminal 1)

```bash
cd backend
pip install -r requirements.txt
python main.py
```

✅ Backend runs at: **http://localhost:8000**
📚 API docs at: **http://localhost:8000/docs**

### Frontend Setup (Terminal 2)

```bash
cd frontend
npm install
npm run dev
```

✅ Frontend opens at: **http://localhost:5173**

---

## ✨ Test It

1. Open **http://localhost:5173** in your browser
2. Fill in the form with {ENTITY_NAME_LOWER} details
3. Click "Add {ENTITY_NAME}"
4. See it appear in the list below
5. Click "Delete" to remove items

**You have a working full-stack app!** 🎉

---

## 📦 What's Inside

- **Backend**: FastAPI server with CRUD API endpoints
- **Frontend**: React 19 app with Vite
- **Storage**: In-memory (no database setup needed)
- **Styling**: Clean, responsive CSS

---

## 🔌 API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/` | Health check |
| GET | `/{ENTITY_NAME_LOWER}` | Get all {ENTITY_NAME_LOWER}s |
| POST | `/{ENTITY_NAME_LOWER}` | Create new {ENTITY_NAME_LOWER} |
| GET | `/{ENTITY_NAME_LOWER}/{id}` | Get one {ENTITY_NAME_LOWER} |
| DELETE | `/{ENTITY_NAME_LOWER}/{id}` | Delete {ENTITY_NAME_LOWER} |

---

## 🛠️ Tech Stack

- **Backend**: Python, FastAPI, Uvicorn, Pydantic
- **Frontend**: React 19, Vite, Axios
- **Validation**: Pydantic models
- **Error Handling**: Comprehensive try-catch blocks

---

## 📝 Notes

- Data is stored **in-memory** (resets when you restart the server)
- CORS is enabled for all origins (development mode)
- No authentication required (add if needed for production)

---

**Built with ❤️ by Zero-Code AI App Builder**

Need help? Check the [main project README](https://github.com/yourusername/zero-code-builder)
